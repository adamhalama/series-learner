# Local Convex development (created/updated by `convex dev --local`)
CONVEX_DEPLOYMENT=anonymous:your-local-deployment
NEXT_PUBLIC_CONVEX_URL=http://127.0.0.1:3210
NEXT_PUBLIC_CONVEX_SITE_URL=http://127.0.0.1:3211

# Vercel production/preview:
# Add `CONVEX_DEPLOY_KEY` as a sensitive Vercel environment variable.
# Do not commit deploy keys or include them in URLs.

# Optional lightweight app gate (no user accounts).
# If APP_GATE_PASSWORD is set, APP_GATE_SECRET is required.
APP_GATE_PASSWORD=
APP_GATE_SECRET=
